<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IPC"
        tools:targetApi="31">

        <activity
            android:name=".ui.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.IPC.Splash"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.IPC"
            android:windowSoftInputMode="adjustResize" />

        <!-- adjustPan: o sistema levanta o conteúdo inteiro sem resize da janela -->
        <!-- O WindowInsets manual trata do bottomNavWrapper e do RecyclerView    -->
        <activity
            android:name=".MainActiviy"
            android:exported="false"
            android:theme="@style/Theme.IPC"
            android:windowSoftInputMode="adjustPan|stateHidden" />

        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.IPC"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.UserProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.IPC"
            android:windowSoftInputMode="adjustResize" />

    </application>

</manifest>